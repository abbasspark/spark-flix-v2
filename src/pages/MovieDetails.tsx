import React from 'react';
import MediaPlayer from '../components/Player';

const MovieDetails = () => {
    return (
        <div>
            <MediaPlayer />
        </div>
    );
};

export default MovieDetails;
